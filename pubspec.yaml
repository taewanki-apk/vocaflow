name: vocaflow

description: A Flutter web project for VocaFlow

version: 1.0.0

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.10.0
  firebase_auth: ^3.3.14
  cloud_firestore: ^3.1.16
  flutter_bloc: ^8.0.0
  provider: ^6.0.1
  hooks_riverpod: ^1.0.0

# Add additional packages required for the VocaFlow project here

# Ensure you check the latest versions available on pub.dev.

# Example: Uncomment to add additional packages
#  http: ^0.14.0
#  dio: ^4.0.4


# Development dependencies
dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.7
  json_serializable: ^6.0.1
  mockito: ^5.0.0
